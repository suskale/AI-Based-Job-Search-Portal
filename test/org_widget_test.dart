import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets("Sign up for organization", (WidgetTester tester) async {});
  testWidgets("Login as organization", (WidgetTester tester) async {});
  testWidgets("Create a new job opening", (WidgetTester tester) async {});
  testWidgets("View the applicants", (WidgetTester tester) async {});
}
